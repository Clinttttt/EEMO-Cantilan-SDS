<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Helper\**" />
    <EmbeddedResource Remove="Helper\**" />
    <None Remove="Helper\**" />
  </ItemGroup>

  <!-- No package references. Microsoft.Extensions.Identity.Core was removed once the user entities stopped hashing
       passwords themselves: the domain states what changes when a password changes, and HOW it is hashed is an
       infrastructure choice behind IPasswordHasher. Domain now depends on nothing outside the BCL. -->

</Project>
