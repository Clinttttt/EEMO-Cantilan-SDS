@page "/tcc"
@page "/facility/tcc"
@rendermode InteractiveServer
@inject NavigationManager Navigation

<PageTitle>EEMO Admin — Tampak Commercial Center</PageTitle>

<div class="admin-layout">
    <Sidebar />

    <main class="admin-main">

        <!-- Topbar -->
        <header class="topbar">
            <div class="topbar-left flex gap-2">
                <a href="/menu" class="tcc-back-btn">
                    <svg viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6" /></svg>
                </a>
                <div>
                    <div class="topbar-title">Tampak Commercial Center</div>
                    <div class="topbar-breadcrumb">
                        <a href="/menu" class="breadcrumb-link">Dashboard</a>
                        <span class="breadcrumb-sep">/</span>
                        <span class="breadcrumb-active">TCC — @DateTime.Now.ToString("MMMM yyyy")</span>
                    </div>
                </div>
            </div>
            <div class="topbar-right">
                <div class="topbar-date">
                    <svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="18" rx="2" /><line x1="16" y1="2" x2="16" y2="6" /><line x1="8" y1="2" x2="8" y2="6" /><line x1="3" y1="10" x2="21" y2="10" /></svg>
                    @DateTime.Now.ToString("MMMM d, yyyy")
                </div>
                <div class="topbar-admin">
                    <div class="admin-avatar">A</div>
                    <div class="admin-info">
                        <div class="admin-name">Administrator</div>
                        <div class="admin-role">EEMO Office</div>
                    </div>
                </div>
            </div>
        </header>

        <div class="content-area">

            <!-- ── Hero Banner ── -->
            <div class="tcc-hero">
                <div class="tcc-hero-left">
                    <div class="tcc-hero-icon">
                        <svg viewBox="0 0 24 24"><rect x="2" y="7" width="20" height="14" rx="2" /><path d="M16 7V5a2 2 0 00-2-2h-4a2 2 0 00-2 2v2" /><line x1="12" y1="12" x2="12" y2="16" /><line x1="10" y1="14" x2="14" y2="14" /></svg>
                    </div>
                    <div>
                        <div class="tcc-hero-eyebrow">Facility · TCC</div>
                        <div class="tcc-hero-title">Tampak Commercial Center</div>
                        <div class="tcc-hero-sub">Municipality of Cantilan, Surigao del Sur</div>
                    </div>
                </div>
                <div class="tcc-hero-stats">
                    <div class="tcc-hero-stat">
                        <div class="tcc-hero-val">₱@TotalCollected.ToString("N0")</div>
                        <div class="tcc-hero-key">Collected</div>
                    </div>
                    <div class="tcc-hero-divider"></div>
                    <div class="tcc-hero-stat">
                        <div class="tcc-hero-val">₱@TotalPending.ToString("N0")</div>
                        <div class="tcc-hero-key">Pending</div>
                    </div>
                    <div class="tcc-hero-divider"></div>
                    <div class="tcc-hero-stat">
                        <div class="tcc-hero-val">@CollectionRate%</div>
                        <div class="tcc-hero-key">Collection Rate</div>
                    </div>
                    <div class="tcc-hero-divider"></div>
                    <div class="tcc-hero-stat">
                        <div class="tcc-hero-val">@ActiveStalls.Count()</div>
                        <div class="tcc-hero-key">Total Stalls</div>
                    </div>
                </div>
                <div class="tcc-hero-progress">
                    <div class="tcc-hero-progress-fill" style="width:@CollectionRate%"></div>
                </div>
            </div>

            <!-- ── Toolbar ── -->
            <div class="page-toolbar">
                <div class="toolbar-left">
                    <div class="search-box">
                        <svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="8" /><line x1="21" y1="21" x2="16.65" y2="16.65" /></svg>
                        <input type="text" placeholder="Search stall no. or occupant…"
                               @bind="SearchQuery" @bind:event="oninput" />
                        @if (!string.IsNullOrEmpty(SearchQuery))
                        {
                            <span class="search-clear" @onclick="() => SearchQuery = string.Empty">×</span>
                        }
                    </div>
                    <div class="filter-tabs">
                        @foreach (var f in StatusFilters)
                        {
                            <div class="filter-tab @(ActiveStatus == f ? "active" : "")"
                                 @onclick="() => ActiveStatus = f">
                                @f
                            </div>
                        }
                    </div>
                </div>
                <div style="display:flex;gap:8px;">
                    <a href="/vendors?facility=tcc" class="btn-outline">
                        <svg viewBox="0 0 24 24" style="width:13px;height:13px;stroke:currentColor;fill:none;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;vertical-align:middle;margin-right:4px;"><path d="M11 4H4a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2v-7" /><path d="M18.5 2.5a2.121 2.121 0 013 3L12 15l-4 1 1-4 9.5-9.5z" /></svg>
                        Manage Vendors
                    </a>
                </div>
            </div>

            <!-- ── Info Bar ── -->
            <div class="tcc-section-info">
                <div class="tcc-section-info-left">
                    <div class="tcc-section-label">All Stalls</div>
                    <div class="tcc-section-desc">Monthly rental · 3-year contracts (started Jun 7, 2023) · Stall sizes: 10.5, 17.5, 35, 70 sqm</div>
                    <div class="fee-chips" style="margin-top:8px;">
                        <span class="fee-chip">Rental ₱900–₱4,800/mo</span>
                        <span class="fee-chip">Electricity</span>
                        <span class="fee-chip">Water</span>
                    </div>
                </div>
                <div class="tcc-section-mini-stats">
                    <div class="tcc-mini-stat">
                        <div class="tcc-mini-val">@FilteredStalls.Count(s => s.IsPaid)</div>
                        <div class="tcc-mini-key">Paid</div>
                    </div>
                    <div class="tcc-mini-stat unpaid-stat">
                        <div class="tcc-mini-val">@FilteredStalls.Count(s => !s.IsPaid && !s.IsPartial)</div>
                        <div class="tcc-mini-key">Unpaid</div>
                    </div>
                    <div class="tcc-mini-stat partial-stat">
                        <div class="tcc-mini-val">@FilteredStalls.Count(s => s.IsPartial)</div>
                        <div class="tcc-mini-key">Partial</div>
                    </div>
                    <div class="tcc-mini-stat">
                        <div class="tcc-mini-val">₱@FilteredStalls.Where(s => s.IsPaid).Sum(s => s.MonthlyRate).ToString("N0")</div>
                        <div class="tcc-mini-key">Collected</div>
                    </div>
                </div>
            </div>

            <!-- ── Stalls Table ── -->
            <div class="panel">
                @if (!FilteredStalls.Any())
                {
                    <div class="empty-state">
                        <svg viewBox="0 0 24 24"><rect x="2" y="7" width="20" height="14" rx="2" /><path d="M16 7V5a2 2 0 00-2-2h-4a2 2 0 00-2 2v2" /></svg>
                        <div class="empty-title">No Stalls Found</div>
                        <div class="empty-sub">Try adjusting the filters or search.</div>
                    </div>
                }
                else
                {
                    <div class="table-wrap">
                        <table class="data-table">
                            <thead>
                                <tr>
                                    <th>Stall No.</th>
                                    <th>Actual Occupant</th>
                                    <th>Name on Contract</th>
                                    <th>Area (sqm)</th>
                                    <th>Contract Date</th>
                                    <th>Monthly Rent</th>
                                    <th>OR No.</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var s in FilteredStalls)
                                {
                                    <tr class="@GetRowStatus(s)">
                                        <td>
                                            @s.StallNo
                                            @if (s.IsCorner)
                                            {
                                                <span class="corner-badge">Corner</span>
                                            }
                                        </td>
                                        <td>@s.ActualOccupant</td>
                                        <td>
                                            @if (string.IsNullOrEmpty(s.ContractName) || s.ContractName.StartsWith("No contract"))
                                            {
                                                <span>No contract</span>
                                            }
                                            else
                                            {
                                                @s.ContractName
                                            }
                                        </td>
                                        <td>@(s.AreaSqm > 0 ? $"{s.AreaSqm}" : "—")</td>
                                        <td>@(s.ContractDate.HasValue? s.ContractDate.Value.ToString("MMM d, yyyy") : "—")</td>
                                        <td>₱@s.MonthlyRate.ToString("N0")</td>
                                        <td>
                                            <span class="mono">@(string.IsNullOrEmpty(s.ORNumber) ? "—" : s.ORNumber)</span>
                                        </td>
                                        <td>
                                            <div class="action-btns">
                                                <button class="action-btn" title="View / Record Payment" @onclick="() => OpenPayment(s)">
                                                    <svg viewBox="0 0 24 24"><line x1="12" y1="1" x2="12" y2="23" /><path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6" /></svg>
                                                </button>
                                                <button class="action-btn" title="Payment History" @onclick="() => OpenHistory(s)">
                                                    <svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z" /><polyline points="14 2 14 8 20 8" /><line x1="16" y1="13" x2="8" y2="13" /><line x1="16" y1="17" x2="8" y2="17" /></svg>
                                                </button>
                                                <button class="action-btn" title="View Profile" @onclick="() => OpenEdit(s)">
                                                    <svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2" /><circle cx="12" cy="7" r="4" /></svg>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
            </div>

        </div><!-- end content-area -->
    </main>
</div>


<!-- ══════════════════════════════════════════════
     RECORD PAYMENT MODAL
══════════════════════════════════════════════ -->
@if (ShowPayment && PaymentStall is not null)
{
    <div class="eemo-modal-overlay" @onclick="ClosePayment">
        <div class="eemo-modal" @onclick:stopPropagation="true">
            <div class="eemo-modal-header">
                <div>
                    <div class="eemo-modal-title">Payment Record</div>
                    <div class="eemo-modal-sub">Stall @PaymentStall.StallNo · @PaymentStall.ActualOccupant · @DateTime.Now.ToString("MMMM yyyy")</div>
                </div>
                <button class="eemo-modal-close" @onclick="ClosePayment">
                    <svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18" /><line x1="6" y1="6" x2="18" y2="18" /></svg>
                </button>
            </div>
            <div class="eemo-modal-body">

                <!-- Status Badge -->
                <div style="padding:12px 16px;border-radius:8px;text-align:center;margin-bottom:16px;
                            background:@(PaymentStall.IsPaid ? "var(--green-bg)" : PaymentStall.IsPartial ? "rgba(184, 115, 51, 0.08)" : "var(--red-bg)");">
                    <div style="font-size:12px;font-weight:600;text-transform:uppercase;letter-spacing:.05em;
                               color:@(PaymentStall.IsPaid ? "var(--green)" : PaymentStall.IsPartial ? "#b87333" : "var(--red)");">
                        Status: @(PaymentStall.IsPaid ? "Paid" : PaymentStall.IsPartial ? $"Partial — ₱{PaymentStall.PartialAmount:N0}" : "Unpaid")
                    </div>
                </div>

                <!-- Monthly Rental -->
                <div class="pay-fee-block">
                    <div class="pay-fee-header">
                        <span class="pay-fee-label">Monthly Rental</span>
                        <span class="pay-fee-rate">₱@PaymentStall.MonthlyRate.ToString("N0")/mo · @PaymentStall.AreaSqm sqm</span>
                    </div>
                    @if (PaymentStall.IsPaid)
                    {
                        <div class="pay-record-row">
                            <span class="pay-record-key">Amount Due</span>
                            <span class="pay-record-val">✓ ₱@PaymentStall.MonthlyRate.ToString("N0")</span>
                        </div>
                    }
                    else if (PaymentStall.IsPartial)
                    {
                        <div class="pay-record-row">
                            <span class="pay-record-key">Amount Due</span>
                            <span class="pay-record-val">₱@PaymentStall.MonthlyRate.ToString("N0")</span>
                        </div>
                        <div class="pay-record-row">
                            <span class="pay-record-key">Amount Paid</span>
                            <span class="pay-record-val">✓ ₱@PaymentStall.PartialAmount.ToString("N0")</span>
                        </div>
                        <div class="pay-record-row">
                            <span class="pay-record-key">Balance Remaining</span>
                            <span class="pay-record-val">⚠ ₱@((PaymentStall.MonthlyRate - PaymentStall.PartialAmount).ToString("N0"))</span>
                        </div>
                    }
                    else
                    {
                        <div class="pay-record-row">
                            <span class="pay-record-key">Amount Due</span>
                            <span class="pay-record-val">✗ ₱@PaymentStall.MonthlyRate.ToString("N0")</span>
                        </div>
                    }
                </div>

                <!-- Financial Summary/Breakdown -->
                <div style="margin-top:20px;padding:16px;border-radius:8px;background:var(--bg-card);border:1px solid var(--border);">
                    <div style="font-size:13px;font-weight:600;text-transform:uppercase;letter-spacing:.05em;color:var(--navy);margin-bottom:12px;">
                        Financial Breakdown
                    </div>
                    <div style="display:flex;justify-content:space-between;padding:8px 0;font-size:13px;border-bottom:2px solid var(--border);margin-bottom:8px;">
                        <span>Monthly Rental:</span>
                        <span style="font-weight:600;">₱@PaymentStall.MonthlyRate.ToString("N0")</span>
                    </div>
                    <div style="display:flex;justify-content:space-between;padding:8px 0;font-size:13px;">
                        <span>Total Paid:</span>
                        <span style="font-weight:600;">₱@PaymentStall.TotalPaid.ToString("N0")</span>
                    </div>
                    <div style="display:flex;justify-content:space-between;padding:8px 0;font-size:13px;">
                        <span>Balance Due:</span>
                        <span style="font-weight:600;">₱@PaymentStall.BalanceDue.ToString("N0")</span>
                    </div>
                </div>

            </div>
            <div class="eemo-modal-footer">
                <button class="btn-ghost" @onclick="ClosePayment">Close</button>
            </div>
        </div>
    </div>
}


<!-- ══════════════════════════════════════════════
     PAYMENT HISTORY MODAL
══════════════════════════════════════════════ -->
@if (ShowHistory && HistoryStall is not null)
{
    <div class="eemo-modal-overlay" @onclick="() => ShowHistory = false">
        <div class="eemo-modal eemo-modal-wide" @onclick:stopPropagation="true">
            <div class="eemo-modal-header">
                <div>
                    <div class="eemo-modal-title">@HistoryStall.ActualOccupant</div>
                    <div class="eemo-modal-sub">Stall @HistoryStall.StallNo · ₱@HistoryStall.MonthlyRate.ToString("N0")/month</div>
                </div>
                <button class="eemo-modal-close" @onclick="() => ShowHistory = false">
                    <svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18" /><line x1="6" y1="6" x2="18" y2="18" /></svg>
                </button>
            </div>
            <div class="eemo-modal-body">
                <div class="hist-label">12-Month Payment History</div>
                <div class="hist-grid">
                    @for (int i = 11; i >= 0; i--)
                    {
                        var month = DateTime.Now.AddMonths(-i);
                        var key = month.ToString("yyyy-MM");
                        var paid = HistoryStall.PaymentHistory.ContainsKey(key) && HistoryStall.PaymentHistory[key];
                        <div class="hist-month @(paid ? "hist-paid" : "hist-unpaid")">
                            <div class="hist-month-name">@month.ToString("MMM")</div>
                            <div class="hist-month-year">@month.ToString("yy")</div>
                            <div class="hist-month-icon">@(paid ? "✓" : "✗")</div>
                        </div>
                    }
                </div>
                <div class="hist-summary">
                    <div class="act-stat">
                        <div class="act-stat-val">@HistoryStall.PaymentHistory.Count(p => p.Value)</div>
                        <div class="act-stat-key">Months Paid</div>
                    </div>
                    <div class="act-stat">
                        <div class="act-stat-val">@(12 - HistoryStall.PaymentHistory.Count(p => p.Value))</div>
                        <div class="act-stat-key">Months Unpaid</div>
                    </div>
                    <div class="act-stat">
                        <div class="act-stat-val">₱@(HistoryStall.MonthlyRate* HistoryStall.PaymentHistory.Count(p => p.Value)).ToString("N0")</div>
                        <div class="act-stat-key">Total Collected</div>
                    </div>
                    <div class="act-stat" style="@(HistoryStall.PaymentHistory.Any(p => !p.Value) ? "background:var(--red-bg);" : "")">
                        <div class="act-stat-val" style="color:var(--red)">₱@(HistoryStall.MonthlyRate* HistoryStall.PaymentHistory.Count(p => !p.Value)).ToString("N0")</div>
                        <div class="act-stat-key">Balance Due</div>
                    </div>
                </div>
            </div>
            <div class="eemo-modal-footer">
                <button class="btn-ghost" @onclick="() => ShowHistory = false">Close</button>
            </div>
        </div>
    </div>
}


@code {

    string SearchQuery = string.Empty;
    string ActiveStatus = "All";
    readonly List<string> StatusFilters = new() { "All", "Paid", "Partial", "Unpaid", "Closed" };

    protected override void OnInitialized()
    {
        ResetState();
    }

    private void ResetState()
    {
        SearchQuery = string.Empty;
        ActiveStatus = "All";
        ShowPayment = false;
        PaymentStall = null;
        ShowHistory = false;
        HistoryStall = null;
    }

    class TccStall
    {
        public int Id { get; set; }
        public string StallNo { get; set; } = string.Empty;
        public string ActualOccupant { get; set; } = string.Empty;
        public string ContractName { get; set; } = string.Empty;
        public double AreaSqm { get; set; }
        public bool IsCorner { get; set; }
        public DateTime? ContractDate { get; set; }
        public decimal MonthlyRate { get; set; }
        public bool HasElectricity { get; set; } = true;
        public bool HasWater { get; set; } = true;
        public bool IsPaid { get; set; }
        public bool IsPartial { get; set; }
        public decimal PartialAmount { get; set; }
        public bool IsActive { get; set; } = true;
        public string ORNumber { get; set; } = string.Empty;
        public string CollectorName { get; set; } = string.Empty;
        public decimal ElecAmount { get; set; }
        public decimal WaterAmount { get; set; }
        public decimal TotalBill => MonthlyRate + ElecAmount + WaterAmount;
        public decimal TotalPaid => IsPaid ? TotalBill : IsPartial ? PartialAmount : 0;
        public decimal BalanceDue => TotalBill - TotalPaid;
        public Dictionary<string, bool> PaymentHistory { get; set; } = new();
    }

    List<TccStall> Stalls = new()
    {
        new() { Id=1,  StallNo="T-01", ActualOccupant="Reyes, Ma. Cristina B.",   ContractName="Ma. Cristina B. Reyes",  AreaSqm=10.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=900,  ElecAmount=320,  WaterAmount=130, ORNumber="OR-2026-T01",  CollectorName="Clint Reyes",  PaymentHistory=MakeH(12) },
        new() { Id=2,  StallNo="T-02", ActualOccupant="Canoy, Rosario T.",        ContractName="Rosario T. Canoy",        AreaSqm=10.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=900,  ElecAmount=280,  WaterAmount=110, ORNumber="OR-2026-T02",  CollectorName="Ruel Santos",  PaymentHistory=MakeH(11) },
        new() { Id=3,  StallNo="T-03", ActualOccupant="Villanueva, Dante M.",     ContractName="Dante M. Villanueva",     AreaSqm=10.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPartial=true, PartialAmount=600, MonthlyRate=900,  ElecAmount=310,  WaterAmount=120, ORNumber="OR-2026-T03",  CollectorName="Marco Dizon",  PaymentHistory=MakeH(6)  },
        new() { Id=4,  StallNo="T-04", ActualOccupant="Soriano, Lolita F.",       ContractName="Lolita F. Soriano",       AreaSqm=10.5, IsCorner=true,  ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=1200, ElecAmount=410,  WaterAmount=155, ORNumber="OR-2026-T04",  CollectorName="Clint Reyes",  PaymentHistory=MakeH(12) },
        new() { Id=5,  StallNo="T-05", ActualOccupant="Padilla, Enrique S.",      ContractName="Enrique S. Padilla",      AreaSqm=17.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=1500, ElecAmount=520,  WaterAmount=190, ORNumber="OR-2026-T05",  CollectorName="Ruel Santos",  PaymentHistory=MakeH(10) },
        new() { Id=6,  StallNo="T-06", ActualOccupant="Balasbas, Mercy G.",       ContractName="Mercy G. Balasbas",       AreaSqm=17.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPartial=true, PartialAmount=1000, MonthlyRate=1500, ElecAmount=495,  WaterAmount=180, ORNumber="OR-2026-T06",  CollectorName="Marco Dizon",  PaymentHistory=MakeH(4)  },
        new() { Id=7,  StallNo="T-07", ActualOccupant="Fernandez, Joel A.",       ContractName="Joel A. Fernandez",       AreaSqm=17.5, IsCorner=true,  ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=1800, ElecAmount=600,  WaterAmount=220, ORNumber="OR-2026-T07",  CollectorName="Clint Reyes",  PaymentHistory=MakeH(12) },
        new() { Id=8,  StallNo="T-08", ActualOccupant="Quinabo, Teresita L.",     ContractName="Teresita L. Quinabo",     AreaSqm=35.0, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=2800, ElecAmount=980,  WaterAmount=360, ORNumber="OR-2026-T08",  CollectorName="Ruel Santos",  PaymentHistory=MakeH(9)  },
        new() { Id=9,  StallNo="T-09", ActualOccupant="Calibo, Norman D.",        ContractName="Norman D. Calibo",        AreaSqm=35.0, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPartial=true, PartialAmount=2000, MonthlyRate=2800, ElecAmount=920,  WaterAmount=340, ORNumber="OR-2026-T09",  CollectorName="Marco Dizon", PaymentHistory=MakeH(7)  },
        new() { Id=10, StallNo="T-10", ActualOccupant="Closed",                   ContractName="Erlinda C. Orot",         AreaSqm=10.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=false,   IsActive=false, MonthlyRate=900,  ORNumber="", CollectorName="", PaymentHistory=MakeH(2) },
        new() { Id=11, StallNo="T-11", ActualOccupant="Dano, Felicitas R.",       ContractName="Felicitas R. Dano",       AreaSqm=10.5, IsCorner=true,  ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=1200, ElecAmount=395,  WaterAmount=148, ORNumber="OR-2026-T11",  CollectorName="Clint Reyes",  PaymentHistory=MakeH(12) },
        new() { Id=12, StallNo="T-12", ActualOccupant="Gementiza, Ailyn S.",      ContractName="Ailyn S. Gementiza",      AreaSqm=17.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPartial=true, PartialAmount=900, MonthlyRate=1500, ElecAmount=480,  WaterAmount=175, ORNumber="OR-2026-T12",  CollectorName="Ruel Santos",  PaymentHistory=MakeH(5)  },
        new() { Id=13, StallNo="T-13", ActualOccupant="Tabada, Renato M.",        ContractName="Renato M. Tabada",        AreaSqm=35.0, IsCorner=true,  ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=3200, ElecAmount=1100, WaterAmount=410, ORNumber="OR-2026-T13",  CollectorName="Marco Dizon",  PaymentHistory=MakeH(12) },
        new() { Id=14, StallNo="T-14", ActualOccupant="Lacson, Gloria P.",        ContractName="Gloria P. Lacson",        AreaSqm=17.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=1500, ElecAmount=450,  WaterAmount=165, ORNumber="OR-2026-T14",  CollectorName="Ruel Santos",  PaymentHistory=MakeH(12) },
        new() { Id=15, StallNo="T-15", ActualOccupant="Macabali, Edgardo T.",     ContractName="Edgardo T. Macabali",     AreaSqm=17.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPartial=true, PartialAmount=800, MonthlyRate=1500, ElecAmount=430,  WaterAmount=158, ORNumber="OR-2026-T15",  CollectorName="Clint Reyes",  PaymentHistory=MakeH(3)  },
        new() { Id=16, StallNo="T-16", ActualOccupant="Delmo, Caridad N.",        ContractName="Caridad N. Delmo",        AreaSqm=35.0, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=2800, ElecAmount=870,  WaterAmount=320, ORNumber="OR-2026-T16",  CollectorName="Marco Dizon",  PaymentHistory=MakeH(11) },
        new() { Id=17, StallNo="T-17", ActualOccupant="Paurom, Melchor A.",       ContractName="Melchor A. Paurom",       AreaSqm=35.0, IsCorner=true,  ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=3200, ElecAmount=1050, WaterAmount=385, ORNumber="OR-2026-T17",  CollectorName="Ruel Santos",  PaymentHistory=MakeH(12) },
        new() { Id=18, StallNo="T-18", ActualOccupant="Sumatra, Natividad C.",    ContractName="Natividad C. Sumatra",    AreaSqm=70.0, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=false,   MonthlyRate=4800, ElecAmount=1620, WaterAmount=590, ORNumber="",             CollectorName="Clint Reyes",  PaymentHistory=MakeH(8)  },
        new() { Id=19, StallNo="T-19", ActualOccupant="Bohol, Roderick P.",       ContractName="Roderick P. Bohol",       AreaSqm=70.0, IsCorner=true,  ContractDate=new DateTime(2023,6,7), IsPartial=true, PartialAmount=3500, MonthlyRate=4800, ElecAmount=1580, WaterAmount=570, ORNumber="OR-2026-T19",  CollectorName="Ruel Santos", PaymentHistory=MakeH(6)  },
        new() { Id=20, StallNo="T-20", ActualOccupant="Closed",                   ContractName="Almayda, Rona C.",         AreaSqm=17.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=false,   IsActive=false, MonthlyRate=1500, ORNumber="", CollectorName="", PaymentHistory=MakeH(0) },
        new() { Id=21, StallNo="T-21", ActualOccupant="Orillaneda, Joselyn M.",   ContractName="Joselyn M. Orillaneda",   AreaSqm=35.0, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=2800, ElecAmount=850,  WaterAmount=310, ORNumber="OR-2026-T21",  CollectorName="Marco Dizon",  PaymentHistory=MakeH(10) },
        new() { Id=22, StallNo="T-22", ActualOccupant="Rañola, Bernardo S.",      ContractName="Bernardo S. Rañola",      AreaSqm=17.5, IsCorner=false, ContractDate=new DateTime(2023,6,7), IsPaid=true,    MonthlyRate=1500, ElecAmount=460,  WaterAmount=168, ORNumber="OR-2026-T22",  CollectorName="Clint Reyes",  PaymentHistory=MakeH(12) },
    };

    static Dictionary<string, bool> MakeH(int paid)
    {
        var d = new Dictionary<string, bool>();
        for (int i = 11; i >= 0; i--)
            d[DateTime.Now.AddMonths(-i).ToString("yyyy-MM")] = (11 - i) < paid;
        return d;
    }

    IEnumerable<TccStall> ActiveStalls => Stalls.Where(s => s.IsActive);

    decimal TotalCollected => ActiveStalls.Where(s => s.IsPaid || s.IsPartial).Sum(s => s.IsPartial ? s.PartialAmount : s.MonthlyRate);
    decimal TotalPending => ActiveStalls.Where(s => !s.IsPaid).Sum(s => s.MonthlyRate - (s.IsPartial ? s.PartialAmount : 0));
    int CollectionRate => ActiveStalls.Any() ? (int)((double)ActiveStalls.Count(s => s.IsPaid) / ActiveStalls.Count() * 100) : 0;

    IEnumerable<TccStall> FilteredStalls => Stalls.Where(s =>
    {
        bool matchStatus = ActiveStatus switch
        {
            "Paid" => s.IsPaid && !s.IsPartial && s.IsActive,
            "Partial" => s.IsPartial && s.IsActive,
            "Unpaid" => !s.IsPaid && !s.IsPartial && s.IsActive,
            "Closed" => !s.IsActive,
            _ => true
        };
        bool matchSearch = string.IsNullOrWhiteSpace(SearchQuery)
            || s.ActualOccupant.Contains(SearchQuery, StringComparison.OrdinalIgnoreCase)
            || s.StallNo.Contains(SearchQuery, StringComparison.OrdinalIgnoreCase)
            || s.ContractName.Contains(SearchQuery, StringComparison.OrdinalIgnoreCase);
        return matchStatus && matchSearch;
    }).OrderBy(s => s.StallNo);

    string GetRowStatus(TccStall s)
    {
        if (!s.IsActive) return "row-closed";
        if (s.IsPartial) return "row-partial";
        if (s.IsPaid) return "row-paid";
        return "row-unpaid";
    }

    class PaymentForm { public string Status { get; set; } = "Paid"; }

    bool ShowPayment = false;
    TccStall? PaymentStall = null;

    void OpenPayment(TccStall s)
    {
        PaymentStall = s;
        ShowPayment = true;
    }

    void ClosePayment() { ShowPayment = false; PaymentStall = null; }

    bool ShowHistory = false;
    TccStall? HistoryStall = null;
    void OpenHistory(TccStall s) { HistoryStall = s; ShowHistory = true; }
    void OpenEdit(TccStall s) { Navigation.NavigateTo("/profile"); }
}
